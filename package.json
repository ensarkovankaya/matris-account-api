{
  "name": "matris-account-api",
  "version": "1.0.0",
  "description": "Account API for Matris project",
  "main": "index.js",
  "scripts": {
    "test": "mocha -b -r ts-node/register test/**/*.spec.ts test/**/**/*.spec.ts test/**/**/**/*.spec.ts",
    "e2e": "mocha -b -r ts-node/register test/e2e.ts",
    "check": "tslint -p tslint.json",
    "build": "npm run check && tsc",
    "build:clean": "rm -rf ./dist/* && npm run build"
  },
  "author": "Ensar Kovankaya <ensar@kovankaya.com> (http://kovankaya.com)",
  "license": "ISC",
  "dependencies": {
    "class-validator": "^0.8.5",
    "graphql-request": "^1.6.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/express": "^4.16.0",
    "@types/mocha": "^5.2.4",
    "@types/node": "^10.5.2",
    "chai": "^4.1.2",
    "express": "^4.16.3",
    "matris-logger": "^1.1.4",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  }
}
